---
phase: 07-ux-polish-single-drive-mode
plan: 08
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - diskcomp/cli.py
  - README.md
  - tests/test_cli.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "NTFS-on-macOS warning shown in health check output with specific callout format"
    - "Warning includes fix instructions from get_fix_instructions()"
    - "README updated with 'Known Limitations' section"
    - "README section mentions NTFS-on-macOS read-only limitation"
    - "Health check output shows warning message per D-19"
  artifacts:
    - path: diskcomp/cli.py
      provides: "Modified _display_health_result() to show NTFS callout"
    - path: README.md
      provides: "New 'Known Limitations' section documenting NTFS-on-macOS"
    - path: tests/test_cli.py
      provides: "Test verifying NTFS warning in health check output"
  key_links:
    - from: "diskcomp/cli.py _display_health_result()"
      to: "diskcomp/health.py get_fix_instructions()"
      via: "called to get fix text for NTFS-on-macOS warning"
      pattern: "get_fix_instructions.*fstype.*NTFS"
---

<objective>
Enhance health check output to highlight NTFS-on-macOS/Linux read-only limitation; update README with known limitations section.

Purpose: Users on macOS/Linux understand immediately that NTFS drives are read-only and point to fix instructions; sets expectations upfront.
Output: NTFS warning callout in _display_health_result(), README "Known Limitations" section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux-polish-single-drive-mode/07-CONTEXT.md
@diskcomp/cli.py (_display_health_result function)
@diskcomp/health.py (get_fix_instructions function)
@README.md (existing sections for context)
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NTFS-on-macOS/Linux callout in health check output (D-19)</name>
  <files>diskcomp/cli.py, tests/test_cli.py</files>
  <read_first>
    - diskcomp/cli.py (_display_health_result function, starting around line 158)
    - diskcomp/health.py (check_drive_health return type, get_fix_instructions signature)
    - tests/test_cli.py (health check display test patterns)
  </read_first>
  <action>
In diskcomp/cli.py _display_health_result(), add a specific NTFS-on-macOS/Linux callout.

1. In _display_health_result() function, after displaying fstype and writable status (around line 166-169), add:
   ```python
   # Add NTFS-on-macOS/Linux specific callout (D-19)
   if health.fstype == 'NTFS' and sys.platform in ['darwin', 'linux']:
       from diskcomp.health import get_fix_instructions
       fix_text = get_fix_instructions(health.fstype, sys.platform, path)
       ui.warn(f"⚠ NTFS on {sys.platform.title()}: This drive is read-only. Files cannot be deleted from it here.")
       if fix_text:
           ui.warn(f"To fix: {fix_text}")
   ```

   Exact placement: After the "Writable" line (line 168-169), before the benchmark check (line 171).

2. Add unit tests to tests/test_cli.py:
   - test_display_health_result_ntfs_on_macos(): Create mock health with fstype='NTFS', sys.platform='darwin', call _display_health_result(), verify output contains "NTFS on darwin" and "read-only"
   - test_display_health_result_ntfs_on_linux(): Same for sys.platform='linux'
   - test_display_health_result_ntfs_on_windows(): fstype='NTFS', sys.platform='win32', verify NO callout (NTFS is writable on Windows)
   - test_display_health_result_ext4_on_macos(): fstype='ext4', sys.platform='darwin', verify NO NTFS callout

Rationale: D-19 specifies exact format with warning symbol and fix instructions.
  </action>
  <verify>
    <automated>cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_cli.py -xvs -k "health.*ntfs or ntfs.*health" 2>&1 | grep -E "passed|FAILED"</automated>
  </verify>
  <done>
- _display_health_result() checks fstype and platform
- NTFS warning shown only on macOS or Linux (not Windows)
- Warning format: "⚠ NTFS on darwin/linux: This drive is read-only..."
- Fix instructions from get_fix_instructions() appended to warning
- Tests verify NTFS callout on macOS/Linux, absent on Windows
- grep "⚠ NTFS" diskcomp/cli.py or "NTFS on" shows callout code
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 'Known Limitations' section to README (D-20)</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing sections and structure)
  </read_first>
  <action>
In README.md, add a "Known Limitations" section that documents NTFS-on-macOS/Linux limitation.

1. Find a good location in README.md (likely after "Safety Model" or before final sections)

2. Add section:
   ```markdown
   ## Known Limitations

   ### NTFS Drives on macOS and Linux

   NTFS (Windows filesystem) drives are read-only on macOS and Linux by default. This means:
   - diskcomp can **scan** and **identify** duplicates on NTFS drives
   - diskcomp **cannot delete** files from NTFS drives (parent filesystem restriction)

   **Workaround:**
   If you need to delete from an NTFS drive on macOS or Linux, install a third-party NTFS driver:
   - **macOS**: Use [Tuxera NTFS](https://www.tuxera.com/products/ntfs-for-mac/) or [ntfs-3g](https://www.tuxera.com/community/open-source-ntfs-3g/)
   - **Linux**: Install `ntfs-3g` package: `sudo apt install ntfs-3g` (Debian/Ubuntu) or equivalent for your distro

   diskcomp will detect this limitation and warn you during health checks.
   ```

   Rationale: D-20 specifies README mention of NTFS limitation and fix instructions.
  </action>
  <verify>
    <automated>grep -n "Known Limitations" /Users/wilkinsmorales/code/diskcomp/README.md</automated>
  </verify>
  <done>
- README.md contains "Known Limitations" section
- Section documents NTFS-on-macOS/Linux read-only limitation
- Section includes fix instructions (Tuxera, ntfs-3g)
- grep "Known Limitations" README.md returns non-empty result
  </done>
</task>

</tasks>

<verification>
Run NTFS health check tests:
```
cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_cli.py -xvs -k "health.*ntfs or ntfs.*health"
```
All must pass. Also verify README contains "Known Limitations" section:
```
grep -A 10 "Known Limitations" /Users/wilkinsmorales/code/diskcomp/README.md
```
</verification>

<success_criteria>
- _display_health_result() detects NTFS + (macOS or Linux)
- Warning message exact: "⚠ NTFS on darwin/linux: This drive is read-only. Files cannot be deleted from it here."
- Fix instructions appended from get_fix_instructions()
- NTFS callout NOT shown on Windows (NTFS is writable there)
- README "Known Limitations" section exists
- README explains NTFS read-only limitation and points to fixes
- All tests pass; no regressions in health check display
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-polish-single-drive-mode/07-08-SUMMARY.md`
</output>
