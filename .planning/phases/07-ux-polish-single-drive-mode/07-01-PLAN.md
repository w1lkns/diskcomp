---
phase: 07-ux-polish-single-drive-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - diskcomp/ui.py
  - diskcomp/reporter.py
  - tests/test_ui.py
  - tests/test_reporter.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Summary table shows actual drive names (volume label or path segment) instead of 'Keep'/'Other'"
    - "Unique file sizes are correctly tallied and displayed as non-zero MB in summary"
    - "UI accepts keep_label and other_label parameters in show_summary()"
  artifacts:
    - path: diskcomp/ui.py
      provides: "Updated RichProgressUI.show_summary() and ANSIProgressUI.show_summary() with label parameters"
    - path: diskcomp/reporter.py
      provides: "DuplicateClassifier with fixed unique size tallying logic"
    - path: tests/test_ui.py
      provides: "Test coverage for show_summary() with custom labels"
  key_links:
    - from: "diskcomp/ui.py show_summary()"
      to: "diskcomp/reporter.py DuplicateClassifier.classify()"
      via: "summary dict with correct unique_in_keep_size_mb and unique_in_other_size_mb"
      pattern: "show_summary.*summary\\['summary'\\]"
---

<objective>
Fix the summary table display to show actual drive names and correct unique file sizes.

Purpose: Users see meaningful labels (drive names/paths) instead of generic "Keep"/"Other"; unique file counts and sizes are accurate.
Output: Updated show_summary() methods accepting label parameters; DuplicateClassifier correctly tallies unique file sizes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux-polish-single-drive-mode/07-CONTEXT.md
@diskcomp/ui.py
@diskcomp/reporter.py
@diskcomp/types.py
@tests/test_ui.py
@tests/test_reporter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DuplicateClassifier to correctly tally unique file sizes (D-25)</name>
  <files>diskcomp/reporter.py, tests/test_reporter.py</files>
  <read_first>
    - diskcomp/reporter.py (current classify() method, line 48-180)
    - diskcomp/types.py (FileRecord, ScanResult structures)
    - tests/test_reporter.py (existing test patterns)
  </read_first>
  <action>
In diskcomp/reporter.py, fix the DuplicateClassifier.classify() method to correctly calculate unique file sizes.

Current behavior: unique_in_keep_size_mb and unique_in_other_size_mb in the summary dict are likely 0.0 because the unique file processing doesn't sum the size_bytes.

Fix:
1. In the section that builds unique_in_keep list (line 143-150), add a running total for unique keep file sizes:
   ```python
   unique_in_keep_size_bytes = 0
   for hash_val, records in keep_hash_map.items():
       if hash_val not in other_hash_map:
           for keep_rec in records:
               unique_in_keep_size_bytes += keep_rec.size_bytes
               unique_in_keep.append({...})
   ```

2. In the section that builds unique_in_other list (line 132-141), add a running total:
   ```python
   unique_in_other_size_bytes = 0
   for hash_val, records in other_hash_map.items():
       if hash_val not in keep_hash_map:
           for other_rec in records:
               unique_in_other_size_bytes += other_rec.size_bytes
               unique_in_other.append({...})
   ```

3. In the summary dict (line 162-169), change:
   - 'unique_in_keep_size_mb': unique_in_keep_size_bytes / (1024 ** 2)
   - 'unique_in_other_size_mb': unique_in_other_size_bytes / (1024 ** 2)

Rationale: D-25 requires correct byte totals for both unique sets. Current code doesn't sum sizes, leaving summary zeroed.

Add 2-3 unit tests to tests/test_reporter.py that verify:
- unique_in_keep_size_mb > 0 when keep drive has unique files
- unique_in_other_size_mb > 0 when other drive has unique files
- summary sizes match sum of individual file sizes
  </action>
  <verify>
    <automated>cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_reporter.py -xvs -k "unique" 2>&1 | grep -E "passed|FAILED"</automated>
  </verify>
  <done>
- DuplicateClassifier.classify() calculates unique_in_keep_size_mb and unique_in_other_size_mb from file size totals
- New tests verify unique sizes > 0 when files exist
- grep "unique_in_keep_size_mb.*1024" diskcomp/reporter.py shows correct calculation
  </done>
</task>

<task type="auto">
  <name>Task 2: Update show_summary() to accept and display custom drive labels (D-26)</name>
  <files>diskcomp/ui.py, tests/test_ui.py</files>
  <read_first>
    - diskcomp/ui.py (RichProgressUI.show_summary() around line 150, ANSIProgressUI.show_summary() around line 300)
    - tests/test_ui.py (show_summary test patterns)
  </read_first>
  <action>
Update both RichProgressUI and ANSIProgressUI to accept keep_label and other_label parameters in show_summary() method.

1. In RichProgressUI.show_summary() method signature (around line 150):
   Change from:
   ```python
   def show_summary(self, summary: dict):
   ```
   To:
   ```python
   def show_summary(self, summary: dict, keep_label: str = "Keep", other_label: str = "Other"):
   ```

2. Update the table row labels within RichProgressUI.show_summary():
   Change "Unique (Keep)" to f"Unique in {keep_label}" and "Unique (Other)" to f"Unique in {other_label}"
   Exact locations: in the table.add_row() calls that display unique file counts

3. In ANSIProgressUI.show_summary() method signature:
   Apply the same parameter addition (keep_label, other_label with defaults)

4. Update ANSIProgressUI row labels to use f"Unique in {keep_label}" and f"Unique in {other_label}"
   Exact locations: in the print() statements that display the summary rows

5. Add 2-3 tests to tests/test_ui.py:
   - Test RichProgressUI.show_summary() with custom labels (mock Rich's Table output)
   - Test ANSIProgressUI.show_summary() with custom labels (capture stdout)
   - Verify the actual label text appears in output (grep assertion)

Rationale: D-26 requires replacing generic "Keep"/"Other" with actual drive names (volume_label or path basename).
  </action>
  <verify>
    <automated>cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_ui.py -xvs -k "show_summary" 2>&1 | grep -E "passed|FAILED"</automated>
  </verify>
  <done>
- RichProgressUI.show_summary() accepts keep_label and other_label parameters
- ANSIProgressUI.show_summary() accepts keep_label and other_label parameters
- Tests verify custom labels appear in output
- grep "Unique in" diskcomp/ui.py shows both implementations use label parameters
  </done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```
cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_reporter.py tests/test_ui.py -x
```
All tests must pass.
</verification>

<success_criteria>
- DuplicateClassifier correctly tallies unique file sizes (no more 0.0 MB)
- show_summary() accepts and uses custom drive labels in both Rich and ANSI modes
- All existing tests remain green (no regressions)
- grep "unique_in_.*_size_mb.*1024" finds correct calculation in reporter.py
- grep "keep_label" diskcomp/ui.py shows label parameter in both UI classes
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-polish-single-drive-mode/07-01-SUMMARY.md`
</output>
