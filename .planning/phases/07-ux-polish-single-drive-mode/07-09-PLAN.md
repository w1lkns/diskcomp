---
phase: 07-ux-polish-single-drive-mode
plan: 09
type: execute
wave: 4
depends_on: [07-04, 07-05, 07-06, 07-07]
files_modified:
  - diskcomp/cli.py
  - tests/test_cli.py
  - tests/test_integration.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Interactive wizard menu 'Compare two drives' routes to interactive_drive_picker() -> health checks -> scan"
    - "Interactive wizard menu 'Clean up a single drive' routes to single-drive scan"
    - "Both routes flow through the full pipeline: scan -> summary -> action menu -> (optional) deletion"
    - "Two-drive and single-drive modes share the same post-scan UX (summary, next-steps, action menu)"
    - "All 187 existing tests pass; no regressions"
  artifacts:
    - path: diskcomp/cli.py
      provides: "Completed main() with full wizard->scan->summary->action flow for both modes"
    - path: tests/test_cli.py
      provides: "Complete integration tests for both wizard paths"
    - path: tests/test_integration.py
      provides: "End-to-end tests for two-drive and single-drive workflows"
  key_links:
    - from: "diskcomp/cli.py main()"
      to: "diskcomp/cli.py show_first_run_menu()"
      via: "interactive mode starts with menu"
      pattern: "show_first_run_menu"
    - from: "diskcomp/cli.py main() both modes"
      to: "diskcomp/cli.py show_action_menu()"
      via: "post-scan, before optional deletion"
      pattern: "show_action_menu.*duplicate_count"
---

<objective>
Integrate all Phase 7 features into complete end-to-end workflow: startup banner -> first-run menu -> scan (two-drive or single-drive) -> summary -> action menu -> (optional) deletion.

Purpose: Users experience a cohesive product flow from greeting to completion; both two-drive and single-drive modes feel like first-class citizens.
Output: Unified main() that handles both paths; all existing tests pass; new integration tests verify full flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux-polish-single-drive-mode/07-CONTEXT.md
@diskcomp/cli.py (main function, all wizard/summary/action functions)
@tests/test_cli.py (all Phase 7 tests)
@tests/test_integration.py (existing integration test patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire up complete main() flow integrating banner, menu, scan, summary, action menu for both modes</name>
  <files>diskcomp/cli.py, tests/test_cli.py, tests/test_integration.py</files>
  <read_first>
    - diskcomp/cli.py (current main function structure)
    - tests/test_cli.py (all Phase 7 tests written so far)
    - tests/test_integration.py (existing integration test structure)
  </read_first>
  <action>
Complete the main() function in diskcomp/cli.py with unified flow for both two-drive and single-drive modes.

Current state (from Plans 01-08):
- show_startup_banner() defined
- show_first_run_menu() defined
- show_help_guide() defined
- show_plain_language_summary() defined
- show_next_steps() defined
- show_action_menu() defined
- parse_size_value() defined
- group_by_hash_single_drive() defined
- group_by_size_single_drive() defined

Task: Ensure main() logic cleanly routes through all these and handles both paths.

1. Verify main() structure follows this sequence:

   a. Parse args
   b. Validate --min-size if provided (Plan 02)
   c. Handle --undo flag early exit (existing logic)
   d. Handle --delete-from flag (existing logic)
   e. Detect interactive mode (no args)
   f. If interactive:
      - Show banner (Plan 03)
      - Loop with menu until user selects action (Plan 04)
        - Menu '1' (Compare two drives): break out of menu
        - Menu '2' (Clean up single drive): set single_path and break
        - Menu '3' (Help): show help and continue loop
        - Menu '4' (Quit): exit 0
   g. If non-interactive (has --keep/--other or --single):
      - Proceed directly to scan (no banner, no menu)
   h. Handle --keep/--other two-drive scan (existing logic, enhanced):
      - Create UI
      - Run health checks and get drive labels
      - Scan and hash
      - Classify
      - Generate report
      - Show plain-language summary (Plan 05)
      - Show next-steps (Plan 05)
      - Show action menu if duplicates > 0 (Plan 07)
      - If action == 'interactive' or 'batch': run deletion workflow
      - Exit with result
   i. Handle --single single-drive scan (Plan 06, enhanced):
      - Create UI
      - Validate path
      - Scan
      - Size filter candidates (two-pass optimization)
      - Hash candidates
      - Group by hash to identify duplicates
      - Generate report
      - Show plain-language summary (Plan 05, with single-drive mode)
      - Show next-steps (Plan 05)
      - Show action menu if duplicates > 0 (Plan 07)
      - If action == 'interactive' or 'batch': run deletion workflow
      - Exit with result

2. Ensure both paths:
   - Call ui.show_summary() with correct labels (from Plan 01)
   - Call show_plain_language_summary() with correct mode
   - Call show_next_steps() with actual report path
   - Call show_action_menu() and handle deletion workflow
   - Call ui.close() exactly once at end

3. Add comprehensive integration tests to tests/test_cli.py and tests/test_integration.py:
   - test_interactive_mode_two_drives(): Mock menu selection '1', verify flow goes through interactive_drive_picker(), scan, summary
   - test_interactive_mode_single_drive(): Mock menu selection '2', verify flow goes through single-drive scan
   - test_interactive_mode_help(): Mock menu selection '3' then '4', verify help shown and exit
   - test_interactive_mode_quit(): Mock menu selection '4', verify exit 0 immediately
   - test_two_drive_full_flow_no_deletion(): --keep --other --dry-run path, verify banner NOT shown, summary shown, action menu shown
   - test_single_drive_full_flow_no_deletion(): --single path --dry-run, verify banner NOT shown, summary shown, action menu shown
   - test_interactive_action_menu_interactive(): Mock menu '1', then mock action menu '1', verify deletion interactive_mode called
   - test_interactive_action_menu_batch(): Mock menu '1', then mock action menu '2', verify deletion batch_mode called
   - test_interactive_action_menu_exit(): Mock menu '1', then mock action menu '3', verify exit cleanly

Rationale: This plan ensures all Phase 7 features integrate seamlessly and both modes feel like complete products.
  </action>
  <verify>
    <automated>cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_cli.py tests/test_integration.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
- main() detects interactive mode and shows banner (Plan 03)
- Interactive mode shows first-run menu (Plan 04) with all 4 options
- Menu option 1 routes to two-drive flow (existing + Plans 05, 07)
- Menu option 2 routes to single-drive flow (Plan 06 + Plans 05, 07)
- Menu option 3 shows help (Plan 04)
- Menu option 4 exits cleanly
- Two-drive mode shows health checks, summary, next-steps, action menu (Plans 05, 07)
- Single-drive mode shows summary, next-steps, action menu (Plans 05, 06, 07)
- Both modes support action menu routing to deletion workflow (Plan 07)
- All integration tests pass
- grep "show_startup_banner.*show_first_run_menu\|show_plain_language_summary.*show_next_steps\|show_action_menu" diskcomp/cli.py shows full integration
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all 187 existing tests pass and no regressions introduced</name>
  <files>tests/</files>
  <read_first>
    - tests/ directory (all test files)
  </read_first>
  <action>
Run the full test suite to ensure all existing tests pass without regression.

```bash
cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/ -v --tb=short
```

Expected output: All 187 tests pass (may skip some Rich-unavailable tests). If any fail:
- Review failure reason
- Fix root cause in implementation
- Re-run to confirm green
- Document in task output

Rationale: Phase 7 must not break any existing functionality. All prior phases' tests must remain green.
  </action>
  <verify>
    <automated>cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/ --co -q 2>&1 | wc -l</automated>
  </verify>
  <done>
- All tests in tests/ directory enumerated (line count)
- Full pytest run with -v completes without hangs
- grep "passed\|failed\|error" from pytest output shows final result
  </done>
</task>

</tasks>

<verification>
Run full test suite:
```
cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/ -v --tb=short 2>&1 | tail -50
```
Must see: "passed" count >= 187 and "failed" == 0. Also run individually:
```
cd /Users/wilkinsmorales/code/diskcomp && python -m pytest tests/test_cli.py tests/test_integration.py -xvs
```
</verification>

<success_criteria>
- All 187 existing tests pass (or more if new tests added)
- Interactive mode complete flow verified via integration tests
- Two-drive mode complete flow verified via integration tests
- Single-drive mode complete flow verified via integration tests
- Action menu routing to deletion workflow tested
- No regressions; all prior phase tests remain green
- Full pytest run completes cleanly with results summary
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-polish-single-drive-mode/07-09-SUMMARY.md`
</output>
