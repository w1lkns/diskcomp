---
phase: 07.1
plan: 05
type: execute
wave: 5
depends_on: [07.1-01, 07.1-02, 07.1-03, 07.1-04]
files_modified:
  - diskcomp/cli.py
  - tests/test_integration.py
  - tests/test_cli.py
autonomous: false
requirements: []
must_haves:
  truths:
    - "User can complete scan → skip folders → flag files → delete without re-scanning"
    - "All 5 UX improvements work together: constrained input, folder skip, file flagging, back navigation, data preservation"
    - "Back navigation works from every menu/screen except during active scanning/deletion"
    - "Deletion excludes both skipped folders AND flagged files"
    - "No hidden failures: all invalid input is rejected with clear messages"
  artifacts:
    - path: diskcomp/cli.py
      provides: "Complete main() integration with post-scan workflow"
      exports: ["main()"]
    - path: tests/test_integration.py
      provides: "End-to-end workflow tests covering all 5 UX improvements"
      contains: "TestEndToEndWorkflow"
  key_links:
    - from: "main()"
      to: "run_post_scan_menu()"
      via: "after scan completes"
      pattern: "run_post_scan_menu.*NavigationContext"
    - from: "run_post_scan_menu()"
      to: "orchestrate_deletion()"
      via: "user selects delete option"
      pattern: "orchestrate_deletion.*mode"
    - from: "orchestrate_deletion()"
      to: "filter_candidates_by_flags()"
      via: "pre-deletion filtering"
      pattern: "filter_candidates_by_flags.*context"
---

<objective>
End-to-end verification and integration of all 5 UX improvements. Users should experience a seamless post-scan workflow with optional folder skipping, file flagging, back navigation throughout, and final deletion that respects both folder selections and file flags. Add comprehensive integration tests covering multiple complete workflows (skip folders → delete, flag files → delete, back navigation → folder skip → delete, etc.). Add checkpoint for manual verification of UX feel.

Purpose: Ensure all 5 UX improvements work together harmoniously, no edge cases slip through, and user experience is genuinely improved.

Output:
- main() updated to wire run_post_scan_menu() into post-scan flow
- Comprehensive end-to-end integration tests
- Checkpoint for manual UX testing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ux-flow-improvements/07.1-RESEARCH.md
@.planning/phases/07.1-ux-flow-improvements/07.1-01-SUMMARY.md
@.planning/phases/07.1-ux-flow-improvements/07.1-02-SUMMARY.md
@.planning/phases/07.1-ux-flow-improvements/07.1-03-SUMMARY.md
@.planning/phases/07.1-ux-flow-improvements/07.1-04-SUMMARY.md

# Existing codebase patterns
@diskcomp/cli.py (main() function and overall orchestration)
@diskcomp/types.py (NavigationContext, DuplicateResults)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run_post_scan_menu() into main() post-scan flow</name>
  <files>diskcomp/cli.py</files>
  <read_first>
    - diskcomp/cli.py (main() function, existing post-scan flow)
    - diskcomp/types.py (NavigationContext)
    - diskcomp/cli.py (run_post_scan_menu signature from Wave 4)
  </read_first>
  <action>
Modify main() in diskcomp/cli.py to call run_post_scan_menu() in the post-scan flow (after summary is displayed, before deletion).

Current flow (simplified):
1. Scan both drives
2. Display summary
3. Ask "Review deletion?" → if yes, show action menu (old approach)

New flow:
1. Scan both drives
2. Display summary
3. Initialize NavigationContext with scan_results, keep_path, other_path
4. Call run_post_scan_menu(context, ui) — returns outcome string
5. Handle outcome:
   - 'deleted': show success message, return exit code 0
   - 'skipped_deletion': show "Exiting without deletion" message, return exit code 0
   - 'aborted': show abort message (existing D-16 format), return exit code 0

Integration point: Find the location in main() where the post-scan "Review deletion?" prompt appears, and replace it with NavigationContext instantiation + run_post_scan_menu() call.

No changes needed to:
- Pre-scan (drive picking, health checks)
- Scan logic (scanner, hasher)
- Summary display

Docstring: Update main() docstring if needed to reflect post-scan menu addition.
  </action>
  <verify>
    <automated>grep -n "run_post_scan_menu" /Users/wilkinsmorales/code/diskcomp/diskcomp/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - main() calls run_post_scan_menu() after summary display
    - NavigationContext initialized with scan results before calling run_post_scan_menu()
    - Outcomes ('deleted', 'skipped_deletion', 'aborted') handled correctly
    - No changes to pre-scan or scan logic
    - No import errors: `python3 -m diskcomp --help` works
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive end-to-end integration tests</name>
  <files>tests/test_integration.py</files>
  <read_first>
    - tests/test_integration.py (existing test patterns and setup)
    - diskcomp/cli.py (main function, all post-scan workflow functions)
    - diskcomp/types.py (NavigationContext, DuplicateResults)
  </read_first>
  <action>
Add TestEndToEndWorkflow class to tests/test_integration.py with 5 comprehensive end-to-end tests covering all 5 UX improvements:

1. test_e2e_skip_folders_and_delete()
   - Mock input() sequence: folder selection "1,2" → delete mode "4" (or "5")
   - Create real scan results with 3 folders and duplicate files across them
   - Call main() (or run_post_scan_menu directly)
   - Assert skipped folders are excluded from deletion
   - Assert files in non-skipped folders are deleted

2. test_e2e_flag_files_and_delete()
   - Mock input() sequence: file flagging "1,3,5" → delete mode "4"
   - Create scan results with 10 duplicate files
   - Call run_post_scan_menu(context, ui)
   - Assert flagged files are not deleted
   - Assert unflagged files are deleted

3. test_e2e_back_navigation_preserves_state()
   - Mock input() sequence: skip folders "1" (back 'b') → skip folders "1,2" → delete
   - Assert folder selection persists after back navigation
   - Assert final deletion respects final folder selection (1,2 skipped)

4. test_e2e_constrained_input_rejects_invalid()
   - Mock input() sequence: menu "7" (invalid) → "6" (valid, exit)
   - Run run_post_scan_menu()
   - Assert ui.error() called for invalid input
   - Assert loop continues until valid input received
   - Assert no exception raised

5. test_e2e_multiple_navigation_cycles()
   - Mock input() sequence: folder skip → back → file flag → back → folder skip (different selection) → file flag (different selection) → delete
   - Assert all selections are correctly applied to final deletion
   - Assert no re-scanning occurs

Test setup:
- Use real temp directories and files (not mocks) for realistic scan results
- Use unittest.mock for input() and UIHandler
- Create helper to build realistic DuplicateResults with multiple groups and unique files

Assertion strategy:
- Check deletion count matches expected (total files minus skipped folders minus flagged files)
- Check no deleted files are in skipped folders
- Check no deleted files are in flagged_files set
- Verify ui.print() or ui.error() called with expected messages
  </action>
  <verify>
    <automated>python -m pytest tests/test_integration.py::TestEndToEndWorkflow::test_e2e_skip_folders_and_delete -v</automated>
  </verify>
  <acceptance_criteria>
    - TestEndToEndWorkflow class exists with 5 test methods
    - All tests pass (pytest -x -q)
    - Tests cover: folder skip + delete, file flag + delete, back nav + state preservation, constrained input, multi-cycle navigation
    - All 5 UX improvements are validated in tests
    - No import errors
    - Tests use real temp files/folders (not just mocks)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <what-built>
All 5 UX improvements have been implemented and tested:
1. Constrained input validation (prompt_confirm helper)
2. Folder skip selection after hashing
3. File flagging for individual file protection
4. Back navigation from every menu/screen
5. Data preservation across back/forward navigation

The implementation includes:
- NavigationContext @dataclass for state preservation
- parse_selection_input() for menu parsing with range notation support
- show_folder_selection() and show_file_flagging() for user interaction
- display_folder_list() and display_file_list() for Rich and ANSI rendering
- filter_candidates_by_flags() for pre-deletion filtering
- run_post_scan_menu() as the central navigation hub
- Comprehensive unit and integration tests (~25+ new tests)
  </what-built>
  <how-to-verify>
Run the interactive workflow manually to verify UX feel and all features work together:

1. Create two temp directories with some test files:
   ```bash
   mkdir -p /tmp/test_keep /tmp/test_other
   dd if=/dev/zero of=/tmp/test_keep/file1.bin bs=1M count=1
   cp /tmp/test_keep/file1.bin /tmp/test_other/file1.bin
   echo "unique" > /tmp/test_other/unique.txt
   ```

2. Run diskcomp interactively:
   ```bash
   python3 -m diskcomp --keep /tmp/test_keep --other /tmp/test_other
   ```

3. After summary displays, test each UX improvement:
   - **Constrained input:** Try entering "7" at the menu → should re-prompt with error
   - **Folder skip:** Select option 1 → view folder list → enter "1" or "1,2" → verify message
   - **Back navigation:** At folder skip screen, enter "b" → should return to main menu
   - **File flagging:** Select option 2 → view file list with sizes → enter "1,3" → verify flagged status
   - **Data preservation:** Flag files → back to menu → folder skip → back to menu → verify previous flags still set
   - **Final deletion:** Select delete mode → verify flagged/skipped files are excluded

4. Verify no error messages in stdout/stderr (except intentional user prompts)

5. Optional: Run full test suite to confirm no regressions:
   ```bash
   python -m pytest tests/ -x -q
   ```

Expected behavior:
- All menus accept only valid keys
- Empty input causes re-prompt ("Please enter a valid choice")
- Back ('b' key) works at every menu/screen except during scanning/deletion
- State persists when navigating back
- Final deletion respects both folder skip and file flag selections
  </how-to-verify>
  <resume-signal>
When you've tested the workflow, describe:
1. Whether all 5 UX improvements felt smooth in interactive use
2. Any edge cases you discovered (if any)
3. Whether constrained input validation worked as expected
4. Whether back navigation preserved state correctly
5. Whether deletion respected both skipped folders and flagged files

Type "approved" to move forward with Phase 07.1 completion.
  </resume-signal>
</task>

</tasks>

<verification>
After all tasks complete and checkpoint approved:
1. Run: `python -m pytest tests/test_integration.py::TestEndToEndWorkflow -x -q` — all 5 new tests pass
2. Run: `python -m pytest tests/ -x -q` — all tests pass (entire suite, ~250+ tests)
3. Manual verification checkpoint passed (user confirmed UX feel and all features work)
4. No import errors: `python3 -m diskcomp --help` works

Wave 5 complete: Phase 07.1 is fully implemented and verified. All 5 UX improvements are in production-ready state.
</verification>

<success_criteria>
- main() wired to call run_post_scan_menu() in post-scan flow
- NavigationContext initialized with scan results
- Outcomes handled correctly (deleted, skipped_deletion, aborted)
- 5 comprehensive end-to-end integration tests pass
- Manual UX verification checkpoint approved
- All existing tests continue to pass (no regressions)
- All 5 UX improvements working together harmoniously
- Phase 07.1 ready for release
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ux-flow-improvements/07.1-05-SUMMARY.md` documenting:
- Integration points in main() function
- End-to-end test coverage summary
- All 5 UX improvements verified
- Phase 07.1 complete: ready for release or next phase
</output>
