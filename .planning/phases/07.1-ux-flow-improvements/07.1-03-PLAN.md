---
phase: 07.1
plan: 03
type: execute
wave: 3
depends_on: [07.1-01, 07.1-02]
files_modified:
  - diskcomp/cli.py
  - diskcomp/ui.py
  - diskcomp/deletion.py
  - diskcomp/reporter.py
  - tests/test_cli.py
  - tests/test_deletion.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can flag individual files in results review screen"
    - "Flagged files show as '(flagged)' marker in display"
    - "Flagged files are excluded from deletion entirely"
    - "Flagged status persists when navigating back/forward"
    - "File size displayed in parentheses next to filename"
  artifacts:
    - path: diskcomp/cli.py
      provides: "show_file_flagging() function for per-file flag selection"
      exports: ["show_file_flagging()"]
    - path: diskcomp/ui.py
      provides: "display_file_list() method with size and flag status (Rich + ANSI)"
      exports: ["display_file_list()"]
    - path: diskcomp/deletion.py
      provides: "filter_candidates_by_flags() function for deletion filtering"
      exports: ["filter_candidates_by_flags()"]
    - path: diskcomp/reporter.py
      provides: "extract_duplicate_files() helper to get individual file records from results"
      exports: ["extract_duplicate_files()"]
    - path: tests/test_cli.py
      provides: "Unit tests for file flagging logic"
      contains: "test_show_file_flagging"
  key_links:
    - from: "run_post_scan_menu()"
      to: "show_file_flagging()"
      via: "user chooses option 2 (flag files)"
      pattern: "choice == '2'"
    - from: "show_file_flagging()"
      to: "NavigationContext.flagged_files"
      via: "storing flagged file paths"
      pattern: "context.flagged_files.*="
    - from: "DeletionOrchestrator()"
      to: "filter_candidates_by_flags()"
      via: "excluding flagged files before deletion"
      pattern: "filter_candidates_by_flags.*context.flagged_files"
---

<objective>
Implement per-file flagging for the results review screen. Users can mark individual files they want to protect from deletion. Each file shows its size in parentheses (e.g., "file.txt (5.2 MB)"). Flagged files are excluded from deletion and persisted in NavigationContext across back/forward navigation.

Purpose: Fine-grained protection before deletion. Users can flag a few critical files without needing to skip entire folder trees.

Output:
- show_file_flagging() in cli.py for interactive per-file selection
- display_file_list() in UIHandler (Rich + ANSI) with size display
- filter_candidates_by_flags() in deletion.py for pre-deletion filtering
- extract_duplicate_files() in reporter.py for extracting individual files from results
- Unit tests for file flagging logic
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ux-flow-improvements/07.1-RESEARCH.md
@.planning/phases/07.1-ux-flow-improvements/07.1-01-SUMMARY.md
@.planning/phases/07.1-ux-flow-improvements/07.1-02-SUMMARY.md

# Existing codebase patterns
@diskcomp/deletion.py (DeletionOrchestrator, candidate filtering patterns)
@diskcomp/reporter.py (DuplicateResults structure)
@diskcomp/types.py (FileRecord, DuplicateResults)
@diskcomp/ui.py (UIHandler display methods)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extract_duplicate_files() to reporter.py</name>
  <files>diskcomp/reporter.py</files>
  <read_first>
    - diskcomp/reporter.py (entire file, understand DuplicateClassifier and DuplicateResults)
    - diskcomp/types.py (DuplicateResults structure with duplicate_groups, other_files)
  </read_first>
  <action>
Add extract_duplicate_files(results: DuplicateResults) -> List[FileRecord] function to diskcomp/reporter.py. This extracts all individual file records from a DuplicateResults object (from both duplicate groups and unique files) for display in file flagging UI.

Behavior:
1. Take a DuplicateResults object
2. Iterate through results.duplicate_groups (list of lists of FileRecords)
3. Flatten all FileRecords into a single list
4. Add all results.other_files FileRecords to the list
5. Return sorted list of FileRecords (sorted by path for consistency)

Example:
```
DuplicateResults:
  duplicate_groups: [
    [FileRecord(path='keep/a.txt', size_bytes=1048576, ...), FileRecord(path='other/a.txt', ...)],
    [FileRecord(path='keep/pic.jpg', size_bytes=2097152, ...), ...]
  ]
  other_files: [FileRecord(path='other/unique.pdf', ...)]

Returns: [FileRecord(path='keep/a.txt', ...), FileRecord(path='keep/pic.jpg', ...), FileRecord(path='other/a.txt', ...), FileRecord(path='other/unique.pdf', ...)]
```

Docstring: Explain use case (for file flagging UI to display all candidates).
  </action>
  <verify>
    <automated>grep -n "def extract_duplicate_files" /Users/wilkinsmorales/code/diskcomp/diskcomp/reporter.py</automated>
  </verify>
  <acceptance_criteria>
    - extract_duplicate_files() function exists in reporter.py
    - Takes DuplicateResults parameter
    - Returns List[FileRecord]
    - Flattens all duplicate groups and unique files
    - Returns sorted list by path
    - Handles empty results gracefully (returns [])
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add display_file_list() to UIHandler (Rich + ANSI) with size display</name>
  <files>diskcomp/ui.py</files>
  <read_first>
    - diskcomp/ui.py (entire file, understand RichProgressUI and ANSIProgressUI)
    - diskcomp/types.py (FileRecord with size_bytes)
    - diskcomp/ansi_codes.py (ANSI color constants)
  </read_first>
  <action>
Add display_file_list(files: List[FileRecord], flagged_set: set[str]) -> None method to both RichProgressUI and ANSIProgressUI classes in diskcomp/ui.py. This displays a numbered list of files with their sizes and flagged status.

Format for each line:
  "  1) /path/to/file.txt (5.2 MB) [flagged]"

Where [flagged] is shown only if the file path is in flagged_set.

Size conversion: size_bytes / (1024*1024) with 1 decimal place.

RichProgressUI:
- Use Rich's Table with columns: Index, Path, Size, Status
- Use Rich styling (dim for flagged, bold for normal)

ANSIProgressUI:
- Use plain text with ANSI codes
- Use self.codes.dim for flagged files, self.codes.ok for normal
- Format: "  {index}) {path} ({size_mb:.1f} MB) {flag_status}"

Both:
- Add blank line after list
- Call self._section("Files to protect (flag these before deletion)") header
- Method signature: def display_file_list(self, files: List[FileRecord], flagged_set: set[str]) -> None

Docstring: Explain display format and how flagged files are marked.
  </action>
  <verify>
    <automated>grep -n "def display_file_list" /Users/wilkinsmorales/code/diskcomp/diskcomp/ui.py</automated>
  </verify>
  <acceptance_criteria>
    - display_file_list() method exists in both RichProgressUI and ANSIProgressUI
    - Displays numbered list with path, size, and flag status
    - Size shown as "5.2 MB" format (1 decimal place)
    - Flagged files show "[flagged]" marker or dim styling
    - Both classes have same signature and behavior
    - Blank line after list
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add show_file_flagging() to cli.py</name>
  <files>diskcomp/cli.py</files>
  <read_first>
    - diskcomp/cli.py (understand show_folder_selection pattern from Wave 2)
    - diskcomp/types.py (NavigationContext, FileRecord)
    - diskcomp/reporter.py (extract_duplicate_files)
  </read_first>
  <action>
Add show_file_flagging(context: NavigationContext, ui: UIHandler) -> NavigationContext function to diskcomp/cli.py. This is the main entry point for per-file flagging, called from run_post_scan_menu() when user selects option 2.

Behavior:
1. Extract all duplicate files from context.scan_results using extract_duplicate_files()
2. If 0 files found, display message "No files to flag" and return context unchanged
3. If 1+ files found:
   a. Call ui.display_file_list(files, context.flagged_files) to show numbered list
   b. Loop until user makes valid selection or presses 'b' to go back:
      - Prompt: "Enter file numbers to flag (e.g., 1,3,5 or 1-3), or 'b' to go back: "
      - Use prompt_confirm with valid_keys = ['1','2',...,str(len(files)),'b']
      - If 'b' pressed: return context unchanged
      - If numbers: parse with parse_selection_input()
      - Convert selected indices to file paths
      - Add to context.flagged_files set (union with existing flags)
   c. Display confirmation: "Flagged {N} files. These will be excluded from deletion."
   d. Return context

Return: Modified NavigationContext with flagged_files populated/updated

Error handling: If parse_selection_input() raises ValueError, call ui.error() and loop again.

Implementation note: Store flagged_files as absolute paths (use file.path from FileRecord).
  </action>
  <verify>
    <automated>grep -n "def show_file_flagging" /Users/wilkinsmorales/code/diskcomp/diskcomp/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - show_file_flagging() function exists in cli.py
    - Takes context (NavigationContext) and ui (UIHandler)
    - Returns modified NavigationContext
    - Extracts files from context.scan_results using extract_duplicate_files()
    - Displays file list using ui.display_file_list()
    - Accepts numbered input with range notation
    - Handles 'b' key to return without changes
    - Validates input with parse_selection_input()
    - Stores results in context.flagged_files as set of file paths
    - Handles errors gracefully (loop on invalid)
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add filter_candidates_by_flags() to deletion.py</name>
  <files>diskcomp/deletion.py</files>
  <read_first>
    - diskcomp/deletion.py (entire file, understand DeletionOrchestrator structure)
    - diskcomp/types.py (DuplicateResults, FileRecord)
  </read_first>
  <action>
Add filter_candidates_by_flags(results: DuplicateResults, flagged_files: set[str]) -> DuplicateResults function to diskcomp/deletion.py. This removes flagged files from the deletion candidate list before passing to DeletionOrchestrator.

Behavior:
1. Take a DuplicateResults object and a set of flagged file paths
2. Iterate through results.duplicate_groups
3. For each group, filter out files whose path is in flagged_files
4. Remove groups that become empty after filtering
5. Filter results.other_files to exclude flagged files
6. Return a new DuplicateResults with filtered lists

Example:
```
Input:
  duplicate_groups: [[FileRecord(path='a.txt'), FileRecord(path='b.txt')]]
  flagged_files: {'a.txt'}

Output:
  duplicate_groups: [] (empty group removed)
  other_files: [] (no changes in this example)
```

Docstring: Explain purpose (pre-deletion filtering) and behavior.
  </action>
  <verify>
    <automated>grep -n "def filter_candidates_by_flags" /Users/wilkinsmorales/code/diskcomp/diskcomp/deletion.py</automated>
  </verify>
  <acceptance_criteria>
    - filter_candidates_by_flags() function exists in deletion.py
    - Takes DuplicateResults and set[str] parameters
    - Returns modified DuplicateResults
    - Removes flagged files from all groups
    - Removes empty groups after filtering
    - Filters other_files list
    - Handles empty flagged_files set (returns results unchanged)
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Add unit tests for file flagging</name>
  <files>tests/test_cli.py, tests/test_deletion.py</files>
  <read_first>
    - tests/test_cli.py (existing test patterns)
    - tests/test_deletion.py (existing DeletionOrchestrator tests)
    - diskcomp/reporter.py (extract_duplicate_files)
    - diskcomp/deletion.py (filter_candidates_by_flags)
  </read_first>
  <action>
Add 4 test methods across both test files:

In tests/test_cli.py (2 tests):
1. test_extract_duplicate_files()
   - Create mock DuplicateResults with 2 duplicate groups and 1 unique file
   - Call extract_duplicate_files()
   - Assert returns flattened list of all FileRecords
   - Assert total count = 5 (2+2+1)

2. test_show_file_flagging_interactive()
   - Mock input() to return "1,3"
   - Create NavigationContext with scan_results
   - Call show_file_flagging(context, ui)
   - Assert context.flagged_files contains paths [1] and [3]
   - Assert ui.display_file_list() called once

In tests/test_deletion.py (2 tests):
3. test_filter_candidates_by_flags_removes_flagged()
   - Create DuplicateResults with 2 groups: [a.txt, b.txt] and [c.txt]
   - Call filter_candidates_by_flags(results, {'a.txt'})
   - Assert results have 1 group: [b.txt]
   - Assert [c.txt] group untouched

4. test_filter_candidates_by_flags_removes_empty_groups()
   - Create DuplicateResults with 1 group: [a.txt]
   - Call filter_candidates_by_flags(results, {'a.txt'})
   - Assert results.duplicate_groups is empty
   - Assert no IndexError or crash

Use unittest.mock to create mock DuplicateResults and FileRecord objects.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli.py::TestCLI::test_extract_duplicate_files -v</automated>
  </verify>
  <acceptance_criteria>
    - 4 new test methods across test_cli.py and test_deletion.py
    - All tests pass (pytest -x -q)
    - Tests cover: file extraction, interactive flagging, flagged file removal, empty group handling
    - No import errors
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: `python -m pytest tests/test_cli.py tests/test_deletion.py -x -q` — all tests pass (existing + 4 new)
2. Test extraction: `python3 -c 'from diskcomp.reporter import extract_duplicate_files; print(extract_duplicate_files.__doc__)'`
3. Test filtering: `python3 -c 'from diskcomp.deletion import filter_candidates_by_flags; print(filter_candidates_by_flags.__doc__)'`
4. No import errors: `python3 -m diskcomp --help` works

Wave 3 complete: File flagging infrastructure is in place and deletion filtering is ready. Next wave (Wave 4) integrates all components (navigation, back buttons, full workflow) and verifies end-to-end functionality.
</verification>

<success_criteria>
- extract_duplicate_files() flattens all files from DuplicateResults
- display_file_list() renders numbered list with sizes and flagged markers in both Rich and ANSI
- show_file_flagging() allows users to select files to protect, stores in NavigationContext
- filter_candidates_by_flags() removes flagged files before deletion
- 4 comprehensive unit tests pass
- All existing tests continue to pass (no regressions)
- Infrastructure ready for Wave 4 integration
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ux-flow-improvements/07.1-03-SUMMARY.md` documenting:
- File flagging flow diagram
- Functions added and their signatures
- Test coverage summary
- Next wave: full workflow integration (back navigation, menu routing, deletion orchestrator wiring)
</output>
