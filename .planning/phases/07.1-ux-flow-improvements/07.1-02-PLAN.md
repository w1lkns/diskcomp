---
phase: 07.1
plan: 02
type: execute
wave: 2
depends_on: [07.1-01]
files_modified:
  - diskcomp/scanner.py
  - diskcomp/cli.py
  - diskcomp/ui.py
  - tests/test_cli.py
  - tests/test_ui.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "After hashing completes, user sees numbered list of scanned folders"
    - "User can enter folder indices (1,3,5 or 1-3) to skip before proceeding"
    - "Selected folders to skip are persisted in NavigationContext.selected_folders_skip"
    - "Back ('b' key) returns to post-scan menu without losing skip selection"
  artifacts:
    - path: diskcomp/scanner.py
      provides: "get_unique_parent_folders(files) function to extract top-level folders from scan"
      exports: ["get_unique_parent_folders()"]
    - path: diskcomp/cli.py
      provides: "show_folder_selection() and parse_selection_input() functions"
      exports: ["show_folder_selection()", "parse_selection_input()"]
    - path: diskcomp/ui.py
      provides: "display_folder_list() method in UIHandler (Rich + ANSI)"
      exports: ["display_folder_list()"]
    - path: tests/test_cli.py
      provides: "Unit tests for folder selection parsing and validation"
      contains: "test_parse_selection_input"
  key_links:
    - from: "run_post_scan_menu()"
      to: "show_folder_selection()"
      via: "user chooses option 1 (skip folders)"
      pattern: "choice == '1'"
    - from: "show_folder_selection()"
      to: "parse_selection_input()"
      via: "parsing comma/space-separated folder indices"
      pattern: "parse_selection_input.*valid_keys"
    - from: "parse_selection_input()"
      to: "NavigationContext.selected_folders_skip"
      via: "storing selected folder paths"
      pattern: "context.selected_folders_skip.*="
---

<objective>
Implement folder skip selection screen. After hashing completes and user sees the summary, they can navigate to option 1 to select one or more folders to exclude before proceeding to deletion. Uses numbered list with comma-separated input (1,3,5 or 1-3 range notation), stores selections in NavigationContext for later filtering.

Purpose: Users can protect entire directory trees from deletion without having to manually flag individual files.

Output:
- get_unique_parent_folders() in scanner.py
- show_folder_selection() in cli.py
- parse_selection_input() in cli.py with range notation support
- display_folder_list() in UIHandler (both Rich and ANSI)
- Unit tests covering parsing and validation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-ux-flow-improvements/07.1-RESEARCH.md
@.planning/phases/07.1-ux-flow-improvements/07.1-01-SUMMARY.md

# Existing codebase patterns
@diskcomp/scanner.py (for file collection patterns)
@diskcomp/ui.py (for display methods)
@diskcomp/cli.py (for menu navigation patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_unique_parent_folders() to scanner.py</name>
  <files>diskcomp/scanner.py</files>
  <read_first>
    - diskcomp/scanner.py (entire file, understand FileRecord and directory walking)
    - diskcomp/types.py (FileRecord structure)
  </read_first>
  <action>
Add get_unique_parent_folders(files: List[FileRecord]) -> List[str] function to diskcomp/scanner.py. This extracts the top-level parent folders from a list of scanned files.

Behavior:
1. Take a list of FileRecord objects from a scan result
2. Extract the parent directory from each file's path using os.path.dirname()
3. Deduplicate by storing in a set
4. Return sorted list of unique parent directory paths
5. Return empty list if no files provided

Example:
```
files: [
  FileRecord(path='/mnt/drive/Photos/pic1.jpg', ...),
  FileRecord(path='/mnt/drive/Photos/pic2.jpg', ...),
  FileRecord(path='/mnt/drive/Videos/video1.mp4', ...),
]

Returns: ['/mnt/drive/Photos', '/mnt/drive/Videos']
```

Docstring: Explain use case (for folder skip selection UI).
  </action>
  <verify>
    <automated>grep -n "def get_unique_parent_folders" /Users/wilkinsmorales/code/diskcomp/diskcomp/scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - get_unique_parent_folders() function exists in scanner.py
    - Takes List[FileRecord] parameter
    - Returns List[str] (folder paths)
    - Deduplicates and sorts results
    - Handles empty list gracefully (returns [])
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add parse_selection_input() to cli.py with range notation</name>
  <files>diskcomp/cli.py</files>
  <read_first>
    - diskcomp/cli.py (understand existing input validation patterns)
    - diskcomp/types.py (NavigationContext)
  </read_first>
  <action>
Add parse_selection_input(input_str: str, max_index: int) -> List[int] function to diskcomp/cli.py. Parses comma/space-separated indices and range notation (e.g., "1-3" means 1,2,3).

Behavior:
1. Accept input like "1,3,5" or "1 3 5" (both comma and space-separated)
2. Accept range notation "1-3" which expands to [1, 2, 3]
3. Validate each index: 1 <= index <= max_index
4. Return sorted list of unique integers
5. Raise ValueError with descriptive message if invalid

Examples of valid input:
- "1,3,5" → [1, 3, 5]
- "1-3" → [1, 2, 3]
- "1,3-5" → [1, 3, 4, 5]
- "1 2 3" → [1, 2, 3]

Examples of invalid input (raise ValueError):
- "" → "No valid selections made"
- "1,7" with max_index=5 → "Index 7 out of range (1-5)"
- "abc" → "Invalid index: abc"
- "1-" → "Invalid range format: 1-"

Docstring: Explain all supported formats and error handling.
  </action>
  <verify>
    <automated>grep -n "def parse_selection_input" /Users/wilkinsmorales/code/diskcomp/diskcomp/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - parse_selection_input() function exists in cli.py
    - Accepts comma, space, and range notation
    - Returns sorted list of unique integers
    - Validates indices against max_index
    - Raises ValueError with clear messages for invalid input
    - Handles edge cases: empty input, out-of-range, malformed ranges
    - Docstring present with examples
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add show_folder_selection() to cli.py and wire to NavigationContext</name>
  <files>diskcomp/cli.py</files>
  <read_first>
    - diskcomp/cli.py (understand prompt_confirm from Wave 1, menu patterns)
    - diskcomp/types.py (NavigationContext, DuplicateResults)
    - diskcomp/scanner.py (get_unique_parent_folders)
  </read_first>
  <action>
Add show_folder_selection(context: NavigationContext, ui: UIHandler) -> NavigationContext function to diskcomp/cli.py. This is the main entry point for folder skip selection, called from run_post_scan_menu() when user selects option 1.

Behavior:
1. Extract unique parent folders from context.scan_results.files using get_unique_parent_folders()
2. If 0 folders found, display message "No folders to skip" and return context unchanged
3. If 1+ folders found:
   a. Call ui.display_folder_list(folders) to show numbered list (use UIHandler method from Task 4)
   b. Loop until user makes valid selection or presses 'b' to go back:
      - Prompt: "Enter folder numbers to skip (e.g., 1,3,5 or 1-3), or 'b' to go back: "
      - Use prompt_confirm with valid_keys = ['1','2',...,str(len(folders)),'b']
      - If 'b' pressed: return context unchanged
      - If numbers: parse with parse_selection_input()
      - Convert selected indices to folder paths
      - Store in context.selected_folders_skip (as set of paths)
   c. Display confirmation: "Selected {N} folders to skip: {folder1}, {folder2}, ..."
   d. Return context

Return: Modified NavigationContext with selected_folders_skip populated

Error handling: If parse_selection_input() raises ValueError, call ui.error() and loop again (do not crash).
  </action>
  <verify>
    <automated>grep -n "def show_folder_selection" /Users/wilkinsmorales/code/diskcomp/diskcomp/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - show_folder_selection() function exists
    - Takes context (NavigationContext) and ui (UIHandler)
    - Returns modified NavigationContext
    - Extracts folders from context.scan_results using get_unique_parent_folders()
    - Displays folder list using ui.display_folder_list()
    - Accepts numbered input with range notation
    - Handles 'b' key to return without changes
    - Validates input with parse_selection_input()
    - Stores results in context.selected_folders_skip as set
    - Handles errors gracefully (loop on invalid, display ui.error())
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add display_folder_list() to UIHandler (Rich + ANSI)</name>
  <files>diskcomp/ui.py</files>
  <read_first>
    - diskcomp/ui.py (entire file, understand RichProgressUI and ANSIProgressUI class structure)
    - diskcomp/ansi_codes.py (ANSI color codes for both UI classes)
  </read_first>
  <action>
Add display_folder_list(folders: List[str]) -> None method to both RichProgressUI and ANSIProgressUI classes in diskcomp/ui.py. This displays a numbered list of folders for user selection.

RichProgressUI implementation:
- Use Rich's Table or Panel with numbered list
- Format: "  1) /path/to/folder1" (with index, folder path)
- Use Rich styling (e.g., cyan text for indices)

ANSIProgressUI implementation:
- Use plain text with ANSI codes (self.codes.bold for indices, self.codes.cyan for paths)
- Format: "  1) /path/to/folder1"
- Use box-drawing characters (optional, but nice to have)

Both:
- Add blank line after list
- Call self._section("Folders to skip") or print header first
- Method signature: def display_folder_list(self, folders: List[str]) -> None

Docstring: Explain display format and styling.
  </action>
  <verify>
    <automated>grep -n "def display_folder_list" /Users/wilkinsmorales/code/diskcomp/diskcomp/ui.py</automated>
  </verify>
  <acceptance_criteria>
    - display_folder_list() method exists in both RichProgressUI and ANSIProgressUI
    - Displays numbered list (1) /path, 2) /path, etc.
    - Includes blank line after list
    - RichProgressUI uses Rich styling (bold, cyan)
    - ANSIProgressUI uses ANSI codes for styling
    - Both have same signature and behavior
    - Docstring present
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Add unit tests for folder skip parsing and selection</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py (existing test patterns, mocking patterns)
    - diskcomp/cli.py (parse_selection_input signature)
  </read_first>
  <action>
Add 5 test methods to tests/test_cli.py in TestCLI class:

1. test_parse_selection_input_comma_separated()
   - Call parse_selection_input("1,3,5", max_index=10)
   - Assert returns [1, 3, 5]

2. test_parse_selection_input_range_notation()
   - Call parse_selection_input("1-3,5", max_index=10)
   - Assert returns [1, 2, 3, 5]

3. test_parse_selection_input_space_separated()
   - Call parse_selection_input("1 2 3", max_index=5)
   - Assert returns [1, 2, 3]

4. test_parse_selection_input_invalid_index()
   - Call parse_selection_input("1,7", max_index=5)
   - Assert raises ValueError with message "Index 7 out of range"

5. test_parse_selection_input_empty_string()
   - Call parse_selection_input("", max_index=5)
   - Assert raises ValueError with message "No valid selections made"

Use unittest.mock to mock input() if testing show_folder_selection() (separate test if needed).
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli.py::TestCLI::test_parse_selection_input_comma_separated -v</automated>
  </verify>
  <acceptance_criteria>
    - 5 new test methods in tests/test_cli.py
    - All tests pass (pytest -x -q)
    - Tests cover: comma-separated, range notation, space-separated, invalid index, empty input
    - No import errors
  </acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: `python -m pytest tests/test_cli.py -x -q` — all tests pass (existing + 5 new)
2. Test folder extraction: `python3 -c 'from diskcomp.scanner import get_unique_parent_folders; print(get_unique_parent_folders.__doc__)'`
3. Test parsing: `python3 -c 'from diskcomp.cli import parse_selection_input; print(parse_selection_input("1-3,5", 10))'` — should return [1,2,3,5]
4. No import errors: `python3 -m diskcomp --help` works

Wave 2 complete: Folder skip selection infrastructure is in place. Ready for Wave 3 tasks (file flagging, back navigation integration, and final deletion filtering).
</verification>

<success_criteria>
- get_unique_parent_folders() extracts top-level folders from scanned files
- parse_selection_input() parses comma, space, and range notation correctly
- show_folder_selection() displays folder list, accepts user input, stores in NavigationContext
- display_folder_list() renders folder list in both Rich and ANSI with proper styling
- 5 comprehensive unit tests pass
- All existing tests continue to pass (no regressions)
- Infrastructure ready for Wave 3 to integrate folder skip into deletion filtering
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-ux-flow-improvements/07.1-02-SUMMARY.md` documenting:
- Folder skip selection flow diagram (menu → folder list → selection → confirmation)
- Functions added and their signatures
- Test coverage summary
- Next wave: file flagging (Task 1-2 of Wave 3) and back navigation integration (Task 3 of Wave 3)
</output>
